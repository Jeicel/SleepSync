Place your app icon image (the swan image you uploaded) here as:

assets/icon.png

Recommended: PNG, square (1024x1024) or large (at least 512x512) for best results.

After adding the image, run:

flutter pub get
flutter pub run flutter_launcher_icons:main

This updates Android launcher and adaptive icons using the provided image.
